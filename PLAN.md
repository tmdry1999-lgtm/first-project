# 기능 추가 계획: 급식 체크리스트 + 선생님 로그인 + 학생 DB

## 배경
현재 앱은 [index.html](index.html) / [script.js](script.js) / [server.js](server.js)로 이루어진 순수 JS 웹앱으로, NEIS API에서 학교별 급식 정보를 조회해 보여준다. 학교 선택 정보는 브라우저 `localStorage`에만 저장된다. [my-app](my-app)은 `create-next-app` 기본 템플릿 상태로 아직 커스텀 코드가 없다.

## 하고 싶은 것 (범위 확정)
1. **급식 체크리스트** — 학생이 오늘 급식을 다 먹었는지 3단계로 체크
   - "다 먹었어요!" 😄 (가장 좋음)
   - "노력했어요!" 💪 (보통)
   - "더 노력해봐요!" 😅 (나쁨)
2. **학생 DB** — 선생님이 학생 목록을 서버 DB에 등록/관리
3. **선생님 로그인** — 로그인한 선생님에게만 학생 DB(및 학생별 체크리스트 기록) 조회 화면을 보여줌

## 범위에서 제외한 것
- **서버 집계**(반 전체 급식 만족도 %, 통계 계산 등)는 하지 않는다. 선생님이 저장된 학생별 기록을 그대로 조회만 할 수 있으면 충분하다.
  - 단, "저장된 데이터를 조회"하는 것 자체도 데이터가 서버 DB에 있어야 가능하므로, 백엔드+DB는 필요하다. "집계"는 통계 계산 유무의 차이일 뿐이다.

## 왜 지금 구조로는 안 되는가
현재 `server.js`는 정적 파일만 서빙하는 단순 서버라 로그인/DB 연동이 어렵다. 로그인 + DB + API가 필요하므로 [my-app](my-app) (Next.js) 쪽을 백엔드로 활용하는 방향을 검토 중이다 (아직 확정 아님, 다음 세션에서 논의).

## 추천 구현 순서
1. **학생 DB** — 학생 목록/반 등 기본 스키마 먼저 구축. 이후 체크리스트 기록을 "어떤 학생"에 연결하기 위해 선행되어야 함.
2. **급식 체크리스트** — 학생 DB에 연결된 체크리스트 제출 기능 (누가, 언제, 몇 단계인지 서버에 저장).
3. **선생님 로그인** — 마지막으로 로그인 붙이고, 로그인한 선생님에게 학생 DB + 체크리스트 기록 조회 화면 제공.

## 다음 세션에서 시작할 것
위 추천 순서에 동의하면 "1. 학생 DB" 설계(스키마, 어디에 구현할지 — my-app/Next.js 기반 여부 등)부터 논의 시작.
